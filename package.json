{
  "name": "google-search-bank",
  "version": "1.0.0",
  "description": "A CAP project for Google Search",
  "repository": "<Add your repository here>",
  "license": "UNLICENSED",
  "private": true,
  "dependencies": {
    "@sap-cloud-sdk/http-client": "^4.0.2",
    "@sap/cds": "^9",
    "express": "^4"
  },
  "devDependencies": {
    "@cap-js/cds-typer": ">=0.1",
    "@cap-js/cds-types": "^0.11.0",
    "@cap-js/sqlite": "^2",
    "@sap-cloud-sdk/resilience": "^4.0.2",
    "@sap/cds-dk": ">=8",
    "@types/node": "^24.0.12",
    "tsx": "^4",
    "typescript": "^5"
  },
  "scripts": {
    "build": "tsc",
    "start": "npm run build && node gen/srv/server.js"
  },
  "cds": {
    "requires": {
      "auth": {
        "kind": "dummy"
      },
      "GoogleSearchAPI": {
        "kind": "rest",
        "credentials": {
          "url": "https://www.googleapis.com/customsearch"
        }
      }
    }
  },
  "imports": {
    "#cds-models/*": "./@cds-models/*/index.js"
  },
  "server": {
    "index": true
  }
}
